<h1>My Blog</h1>

<%= render partial: "heavy_tasks/button" if user_signed_in? %>

<% @blog_posts.each do |blog_post| %>
  <div>
    <h2><%= link_to blog_post.title, blog_post %></h2>
    <p><%= blog_post.body %></p>
  </div>
<% end %>